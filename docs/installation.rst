============
Installation
============

At the command line::

    $ easy_install django-iap-auth

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-iap-auth
    $ pip install django-iap-auth
